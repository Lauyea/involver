@page
@model Involver.Pages.Articles.DeleteModel

@{
    ViewData["Title"] = "刪除文章";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="row justify-content-center">
    <div class="col-lg-8 col-md-10">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h1 class="mb-0">刪除確認</h1>
            </div>
            <div class="card-body">
                <h5 class="card-title">你確定要刪除這篇文章嗎？</h5>
                <p class="text-danger">此操作無法復原。</p>
                <hr />
                <dl class="row">
                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Article.Title)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Article.Title)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Article.Profile.UserName)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Article.Profile.UserName)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Article.Content)</dt>
                    <dd class="col-sm-9 ck-content">@Html.Raw(Model.Article.Content.Substring(0, Math.Min(Model.Article.Content.Length, 100))) ...</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Article.UpdateTime)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Article.UpdateTime)</dd>
                </dl>
            </div>
            <div class="card-footer bg-transparent border-danger">
                <form method="post" class="d-flex justify-content-end">
                    <input type="hidden" asp-for="Article.ArticleID" />
                    <a asp-page="./Details" asp-route-id="@Model.Article.ArticleID" class="btn btn-secondary mr-2">取消</a>
                    <input type="submit" value="刪除" class="btn btn-danger" />
                </form>
            </div>
        </div>
    </div>
</div>