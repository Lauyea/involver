@page
@model Involver.Pages.Feedbacks.DeleteModel

@{
    ViewData["Title"] = "刪除意見";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="row justify-content-center">
    <div class="col-lg-8 col-md-10">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h1 class="mb-0">刪除確認</h1>
            </div>
            <div class="card-body">
                <h5 class="card-title">你確定要刪除這則意見嗎？</h5>
                <p class="text-danger">此操作無法復原。</p>
                <hr />
                <dl class="row">
                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Feedback.Title)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Feedback.Title)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Feedback.Profile.UserName)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Feedback.Profile.UserName)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Feedback.Content)</dt>
                    <dd class="col-sm-9 ck-content">@Html.Raw(Model.Feedback.Content.Substring(0, Math.Min(Model.Feedback.Content.Length, 100))) ...</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Feedback.UpdateTime)</dt>
                    <dd class="col-sm-9">@Model.Feedback.UpdateTime.ToShortDateString()</dd>
                </dl>
            </div>
            <div class="card-footer bg-transparent border-danger">
                <form method="post" class="d-flex justify-content-end">
                    <input type="hidden" asp-for="Feedback.ArticleID" />
                    <a asp-page="./Details" asp-route-id="@Model.Feedback.ArticleID" class="btn btn-secondary me-2">取消</a>
                    <input type="submit" value="刪除" class="btn btn-danger" />
                </form>
            </div>
        </div>
    </div>
</div>