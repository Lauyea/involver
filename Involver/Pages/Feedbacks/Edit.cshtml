@page
@model Involver.Pages.Feedbacks.EditModel
@using Microsoft.AspNetCore.Identity
@using Involver.Common

@inject UserManager<InvolverUser> userManager

@{
    ViewData["Title"] = "編輯意見";
    Layout = "~/Pages/Shared/_Layout.cshtml";
    string UserID = userManager.GetUserId(User);
}

<div class="row justify-content-center">
    <div class="col-lg-8 col-md-10">
        <div class="card">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center">
                    <h1 class="mb-0">編輯意見</h1>
                    <div>
                        <a asp-page="./Details" asp-route-id="@Model.Feedback.ArticleID" class="btn btn-sm btn-outline-info"><i class="fas fa-eye"></i> 查看內容</a>
                        <a asp-page="./Index" class="btn btn-sm btn-outline-secondary"><i class="fas fa-list"></i> 回到清單</a>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                    <input type="hidden" asp-for="Feedback.ArticleID" />
                    <input type="hidden" asp-for="Feedback.ProfileID" value="@UserID" />

                    <div class="form-group">
                        <label asp-for="Feedback.Title" class="control-label"></label>
                        <input asp-for="Feedback.Title" class="form-control" />
                        <span asp-validation-for="Feedback.Title" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Feedback.Content" class="control-label"></label>
                        <textarea asp-for="Feedback.Content" id="editor" class="form-control" rows="12"></textarea>
                        <div id="word-count" class="text-right text-muted"></div>
                        <span asp-validation-for="Feedback.Content" class="text-danger"></span>
                    </div>

                    <div class="form-group d-flex justify-content-end">
                        <a asp-page="./Details" asp-route-id="@Model.Feedback.ArticleID" class="btn btn-secondary mr-2">取消</a>
                        <input type="submit" value="儲存" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    @{
        ViewData["EditorId"] = "editor";
        ViewData["WordCountDivId"] = "word-count";
        ViewData["MaxLength"] = Parameters.ArticleLength;
        await Html.RenderPartialAsync("_ClassicEditorPartial");
    }
}